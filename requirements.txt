# Единая команда установки приложения и инструментов разработки.
# Конкретные зависимости и допустимые версии объявлены в pyproject.toml,
# чтобы два списка пакетов не расходились со временем.
-e .[dev]
