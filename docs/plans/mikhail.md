# Михаил — план на 07.09–13.09.2026

Рабочая ветка: `mikhail`. Напарник: Павел, ветка `pavel`.

**Дедлайн всего проекта: 27.09.2026. Этот файл планирует только первую неделю.** Цель недели — реализовать основную статистическую часть, получить первый вариант Adversarial Validation и вместе с Павлом проверить общий сценарий. Полная готовность проекта к 13.09 не требуется.

## Ответственность и договорённости

Твои модули: `drift/`, `adversarial.py`, правила drift-алертов в `alerts.py`, `scripts/generate_demo_data.py`, `notebooks/demo.ipynb`, методические заметки и эксперименты. Тесты своего кода пишешь вместе с реализацией. `contracts.py` и схему конфигурации согласовываете вдвоём.

Каждый день заканчивается короткой отметкой: выполненное, commit/PR, проверка и блокер. Если задача не помещается в день, фиксируй реальное состояние и обсуждай перенос внутри текущей недели. Объём за её пределами пока не планируем.

## 07.09, понедельник — контракт и воспроизводимые данные

**Сделать**

- [ ] Вместе с Павлом разобрать `docs/contracts.md`: согласовать ключи результата, типы признаков, `None`, `skipped`, ошибки схемы и общие сигнатуры. Внести согласованные изменения одним общим PR.
- [ ] Поднять окружение, проверить импорт NumPy/pandas/SciPy/LightGBM. Совместно записать проверенные версии зависимостей и способ установки.
- [ ] Реализовать `scripts/generate_demo_data.py`: Reference и независимый Current без внесённого дрейфа; предусмотреть параметры seed и размера. Минимальные признаки — `age`, `income`, `region` из конфигурации.
- [ ] Добавить сценарии сдвига числового признака, изменения долей категорий и роста пропусков. Файлы писать в игнорируемый `data/generated/`; описать команды и параметры в `data/README.md`.
- [ ] Передать Павлу команды генерации и согласованную схему. Указать фактические типы колонок и единицы измерения.

**Разобраться**

- [ ] Различия Data Quality, data drift и concept drift; что нельзя установить без целевой переменной.
- [ ] Reference и Current как независимые батчи. Почему контроль без дрейфа не должен быть только копией Reference.
- [ ] Как seed обеспечивает повторяемость генератора и почему сдвиг должен вноситься явно, с известными параметрами.

**Готово, если:** повтор генерации с тем же seed даёт те же таблицы; контроль и внесённые изменения описаны; Павел воспроизводит загрузку этих данных.

**Зависимость / передача:** общая схема и пример данных нужны Павлу сегодня для загрузчика. Уведомить его о готовности в вашем обычном рабочем общении.

**Итог дня:** выполнено — …; commit/PR — …; проверено — …; блокеры — …

## 08.09, вторник — KS и Wasserstein

**Сделать**

- [ ] Реализовать `ks_test` и `wasserstein` в `drift/numeric.py` с возвратом общего `CheckResult`.
- [ ] Обрабатывать NaN, бесконечности и недостаток пригодных значений по контракту. Возвращать размеры использованных выборок и причины пропуска.
- [ ] Написать тесты: одинаковые значения, явный сдвиг, пустые/полностью пропущенные значения, константный признак, разные размеры выборок. Для ожидаемых чисел использовать простой известный пример или прямую проверку результата SciPy.
- [ ] Передать Павлу минимальный вызов функций и пример реального результата для будущего pipeline.

**Разобраться**

- [ ] Нулевая гипотеза KS, эмпирическая функция распределения и смысл статистики D.
- [ ] Почему p-value не равно вероятности отсутствия дрейфа и зависит от размера выборки.
- [ ] Почему Wasserstein выражается в единицах признака и общий порог для возраста и дохода требует обоснования.
- [ ] Ограничения KS для дискретных/округлённых значений и повторов.

Материалы: [SciPy KS](https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.ks_2samp.html), [SciPy Wasserstein](https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.wasserstein_distance.html).

**Готово, если:** обе функции возвращают согласованный результат и проходят перечисленные проверки; неподходящие данные не превращаются в утверждение «дрейфа нет».

**Итог дня:** выполнено — …; commit/PR — …; проверено — …; блокеры — …

## 09.09, среда — PSI и Jensen–Shannon

**Сделать**

- [ ] Реализовать общую подготовку бинов/категорий в `drift/binning.py` и расчёты в `drift/stability.py`.
- [ ] Строить числовые границы по Reference; применять одни границы к обеим выборкам. Предусмотреть хвосты и вырожденный Reference.
- [ ] Зафиксировать обработку нулевых частот и новых категорий. Для PSI явно описать выбранное сглаживание и нормировку вероятностей.
- [ ] Для JS считать именно divergence с фиксированным основанием логарифма. Записать математические определения в документацию.
- [ ] Добавить тесты на совпадающие распределения, нулевые ячейки, константный признак, новую категорию и значения Current за пределами Reference.

**Разобраться**

- [ ] Формулы PSI и JS; зачем требуется общее разбиение пространства значений.
- [ ] Почему выбор числа бинов и сглаживания влияет на результат.
- [ ] Разница Jensen–Shannon distance и divergence. При базе 2 divergence лежит между 0 и 1.
- [ ] Почему привычные пороги PSI нельзя автоматически считать доказанной границей для любого датасета.

Материал: [SciPy Jensen–Shannon](https://docs.scipy.org/doc/scipy/reference/generated/scipy.spatial.distance.jensenshannon.html).

**Готово, если:** одинаковые распределения дают нулевую дистанцию/дивергенцию с учётом численной точности, JS при базе 2 остаётся в допустимых границах, крайние случаи обрабатываются явно.

**Зависимость / передача:** готовые числовые методы уже доступны Павлу независимо от завершения всех индексов.

**Итог дня:** выполнено — …; commit/PR — …; проверено — …; блокеры — …

## 10.09, четверг — категориальный тест и правила алертов

**Сделать**

- [ ] Реализовать χ² в `drift/categorical.py` по таблице частот Reference/Current с совместимыми категориями.
- [ ] Описать и реализовать выбранную политику редких категорий. Если условия применимости не выполнены, вернуть `skipped` с причиной; не выдавать необоснованный p-value.
- [ ] Добавить формирование drift-алертов в `alerts.py`: источник, признак, метрика, использованный порог и объяснение.
- [ ] Реализовать согласованную поправку на множественные проверки для пригодных p-value; сохранять исходные и скорректированные значения. Точно определить семейство проверок.
- [ ] Совместно с Павлом запустить первый общий проход: DataFrame → проверка схемы/качества → реализованные статистические методы → словарь результата.

**Разобраться**

- [ ] Наблюдаемые и ожидаемые частоты в χ², степени свободы и ограничения асимптотического теста.
- [ ] Ошибка первого рода, множественные проверки и смысл FDR.
- [ ] Разница между расчётом метрики и правилом решения об алерте; почему отсутствующий порог должен означать отсутствие решения.

Материал: [SciPy χ²](https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.chi2_contingency.html).

**Готово, если:** работают базовые категориальные сценарии и обработка редких значений; общий результат сериализуется в JSON без NaN/Infinity; Павел может показать реальные результаты методов.

**Итог дня:** выполнено — …; commit/PR — …; проверено — …; блокеры — …

## 11.09, пятница — первый Adversarial Validation

**Сделать**

- [ ] Реализовать базовый модуль `adversarial.py`: метки 0/1 для источника строк, LightGBM и стратифицированная кросс-валидация с фиксированным seed.
- [ ] Строить предобработку внутри train-части каждого фолда. Исключить источник выборки, ID и целевую переменную из признаков; корректно обрабатывать новые категории в validation.
- [ ] Получать ROC-AUC по отложенным предсказаниям, значения по фолдам и важности признаков с указанием их типа. Не интерпретировать важность как причинный эффект.
- [ ] Обработать слишком малые батчи, недостаток строк для фолдов и отсутствующие подходящие признаки.
- [ ] Проверить запуск на независимых выборках без сдвига и на явно различимых выборках. Передать Павлу результат по контракту.

**Разобраться**

- [ ] Что означает AUC около 0.5 и высокий AUC в задаче «Reference против Current».
- [ ] Почему оценка на обучающих строках может создавать ложное ощущение дрейфа.
- [ ] Как одинаковые объекты в разных фолдах создают утечку и когда нужен групповой split.
- [ ] Как читать Feature Importance и почему она не доказывает причину изменения данных.

Материалы: [LightGBM](https://lightgbm.readthedocs.io/en/stable/Python-Intro.html), [scikit-learn: утечки](https://scikit-learn.org/stable/common_pitfalls.html).

**Готово, если:** модуль возвращает отложенный AUC и важности в воспроизводимом запуске. Численные результаты записаны как наблюдения; порог AUC не объявлен универсальным.

**Итог дня:** выполнено — …; commit/PR — …; проверено — …; блокеры — …

## 12.09, суббота — первые эксперименты и notebook

**Сделать**

- [ ] Заполнить `notebooks/demo.ipynb`: генерация, загрузка, вызов общего API, просмотр метрик и объяснение результатов.
- [ ] Выполнить базовые сценарии из `docs/experiments.md`: контроль без дрейфа, числовой сдвиг, смена долей категорий и рост пропусков.
- [ ] Для контроля без дрейфа повторить несколько независимых seed и записать наблюдения о ложных срабатываниях. Это первичная проверка, не полноценная калибровка всех порогов.
- [ ] Зафиксировать фактические размеры данных, параметры, версии, метрики, время и commit.
- [ ] Вместе с Павлом сверить значения в notebook, словаре и дашборде; исправить найденные расхождения.

**Разобраться**

- [ ] Ложные тревоги, чувствительность и зависимость результатов от величины сдвига и размера батча.
- [ ] Почему отсутствие срабатывания выбранных тестов не доказывает идентичность всех распределений.
- [ ] Разница между ошибкой данных и изменением валидного распределения.

**Готово, если:** notebook выполняется по порядку для реализованного сценария, результаты имеют воспроизводимое происхождение и согласованы с интерфейсом Павла.

**Итог дня:** выполнено — …; commit/PR — …; проверено — …; блокеры — …

## 13.09, воскресенье — проверка результата недели

**Сделать**

- [ ] Запустить реализованные модули и содержательные тесты из общей ветки. Исправить блокирующие ошибки первой интеграции.
- [ ] Проверить путь Павла: загрузка файлов, ошибки схемы, сводка, графики. Сверить интерпретацию статистических результатов.
- [ ] Объяснить Павлу KS, Wasserstein, PSI, JS, χ² и Adversarial Validation на примерах проекта; попросить его объяснить тебе инженерный путь данных.
- [ ] Объединить проверенные изменения в `main`, обновить свой план и отметить фактическую готовность в `docs/deliverables.md`.
- [ ] Записать оставшиеся вопросы и требования без календарного расписания следующих недель. Отдельно зафиксировать, какие пороги пока исследовательские.

**Разобраться**

- [ ] Какие ограничения уже видны на экспериментах и что система пока не умеет обосновывать.
- [ ] Как объяснить путь от входных таблиц до алерта и какие данные подтверждают его корректность.

**Готово, если:** в `main` находится проверяемый промежуточный результат, напарник может воспроизвести твою часть, незавершённые функции явно отмечены. Финальный отчёт, скринкаст и окончательная готовность остаются требованиями к 27.09, а не условием завершения этой недели.

**Итог дня:** выполнено — …; commit/PR — …; проверено — …; блокеры — …
