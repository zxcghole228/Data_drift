# Михаил — план на 15.09–20.09.2026

Рабочая ветка: `mikhail`. Напарник: Павел, ветка `pavel`.

**Дедлайн всего проекта: 27.09.2026. Этот файл планирует только 15–20 сентября.**
Цель недели — превратить работающий прототип первой недели в проверяемый
кандидат на релиз: сделать пороги признаков предметными, усилить диагностику
категориального и ML-дрейфа, провести серию воспроизводимых экспериментов и
согласовать статистический смысл результатов во всех интерфейсах.

Стартовая точка — change set `636fbe4` после его проверки и слияния в `main`.
Уже реализованные KS, Wasserstein, PSI, JS, χ², BH, Adversarial Validation и
demo-сценарии не переписываются без воспроизводимого теста, показывающего
проблему.

## Ответственность и правила недели

Твоя зона: разрешение порогов drift-метрик, статистические диагностические
величины, `adversarial.py`, генератор и сценарии экспериментов, notebook,
описание методов и ограничений. Павел отвечает за CLI, HTML-отчёт, Streamlit,
Docker и CI. Контракт, конфигурацию и формат отчёта вы утверждаете вдвоём до
изменения публичного API.

- Каждая новая настройка должна проходить runtime-валидацию и иметь тест на
  обратную совместимость.
- Метрика и решение об алерте остаются разными сущностями. Если порог не задан,
  `alert` остаётся `None`, а не превращается в `False`.
- Ни один результат не содержит `NaN`/`Infinity`; входные DataFrame не
  изменяются.
- Порог называется исследовательским, пока его не подтверждают эксперименты и
  предметное правило.
- Работа ведётся короткими ветками и PR от актуального `main`; в конце дня
  фиксируются commit/PR, проверки и блокеры.

При нехватке времени обязательны персональные пороги, эксперименты и общая
интеграция. Cramér's V и групповой split не должны приниматься ценой сломанных
тестов или неясного контракта.

## 15.09, вторник — контракт второй итерации

**Сделать**

- [ ] Вместе с Павлом завершить ревью change set `636fbe4`, объединить его с
  `main` и синхронизировать обе рабочие ветки. Зафиксировать новый базовый SHA.
- [ ] Разобрать ограничения из `docs/week1_handoff.md`: общий порог
  Wasserstein, исследовательские PSI/JS/AUC, отсутствие группового split и
  незавершённый HTML-экспорт.
- [ ] Спроектировать обратно совместимое поле `drift.feature_thresholds`:
  отображение `feature -> {wasserstein, psi, js}`. Зафиксировать приоритет
  `порог признака -> общий порог метода -> None`.
- [ ] Зафиксировать в контракте, что `alpha` и BH остаются общими для семейства
  KS/χ² одного вызова `analyze`; не вводить отдельный alpha для каждого
  признака без новой статистической модели семейства.
- [ ] Определить диагностические поля результата: источник порога
  (`feature`, `global`, `not_configured`), Cramér's V для χ² и стратегия
  разбиения Adversarial Validation.
- [ ] Добавить в контракт top-level `effective_config` — JSON-safe снимок уже
  провалидированной конфигурации, по которой действительно выполнен анализ.
  Повысить `contract_version` до `0.2`, не ломая чтение старого YAML 0.1.
- [ ] Согласовать с Павлом примеры YAML, JSON и критерии приёмки одним общим PR
  до начала реализации.

**Разобраться**

- [ ] Чем статистическая значимость отличается от практической величины
  изменения.
- [ ] Почему Wasserstein измеряется в единицах признака, а PSI/JS безразмерны.
- [ ] Почему обратная совместимость конфигурации важна для CLI, notebook и UI.

**Готово, если:** старая `configs/default.yaml` остаётся допустимой, новый YAML
однозначно определяет порог каждого признака, `effective_config` совпадает с
фактическим запуском, а Павел может по контракту объяснить, какое значение
попадёт в HTML и интерфейс.

**Зависимость / передача:** контракт редактирует один автор, второй делает
review. Реализацию 16.09 не начинать, пока не согласованы названия полей и
приоритеты.

**Итог дня:** выполнено — …; commit/PR — …; проверено — …; блокеры — …

## 16.09, среда — персональные пороги признаков

**Сделать**

- [ ] Реализовать отдельный resolver порога по имени признака и методу, не
  размазывая правила приоритета по `pipeline.py`.
- [ ] Подключить resolver к Wasserstein, PSI и JS. Добавить в `details`
  фактически использованный порог и его источник.
- [ ] Добавить runtime-валидацию: только известные признаки и методы,
  неотрицательные конечные значения либо `None`; неизвестный ключ — ошибка
  конфигурации, а не молчаливое игнорирование.
- [ ] Сохранить поведение старых конфигураций без `feature_thresholds`.
- [ ] Написать тесты на персональный override, общий fallback, отсутствие
  порога, значение ровно на границе и ошибочные настройки.
- [ ] Передать Павлу короткий YAML-пример для CLI и UI и реальный фрагмент
  `CheckResult` с `threshold_source`.

**Разобраться**

- [ ] Почему единый абсолютный порог Wasserstein некорректен одновременно для
  возраста и дохода.
- [ ] Чем строгая граница `>` отличается от `>=` и почему это правило должно
  быть единым во всех интерфейсах.
- [ ] Почему значение метрики без порога остаётся полезным наблюдением, но не
  автоматическим решением.

**Готово, если:** один анализ может применить разные пороги Wasserstein к
`age` и `income`, старый YAML даёт прежний результат, а тесты подтверждают
`alert=None` при отсутствии решения.

**Зависимость / передача:** после PR Павел использует только поля результата и
не повторяет правила выбора порога в CLI или Streamlit.

**Итог дня:** выполнено — …; commit/PR — …; проверено — …; блокеры — …

## 17.09, четверг — сила категориального сдвига

**Сделать**

- [ ] Добавить к успешному χ² стандартный Cramér's V, рассчитанный по той же
  итоговой таблице частот после объединения редких категорий.
- [ ] Оставить χ²-статистику и p-value без изменения; Cramér's V хранить как
  диагностическую величину, не создавая по нему алерт без отдельного порога.
- [ ] Возвращать число исходных, новых, исчезнувших и объединённых категорий,
  а также долю наблюдений в pooled-категории.
- [ ] Явно обработать вырожденную таблицу, единственную категорию и нулевой
  знаменатель: `skipped` с причиной вместо `NaN`.
- [ ] Добавить тесты на одинаковые доли, сильный сдвиг, новую категорию, pooled
  tail и согласованность Cramér's V с ручным расчётом.
- [ ] Обновить `docs/categorical_drift.md`: p-value отвечает на вопрос о
  совместимости распределений, Cramér's V — о размере обнаруженного различия;
  ни один универсальный порог силы эффекта не объявлять.

**Разобраться**

- [ ] Как Cramér's V нормирует χ² относительно числа наблюдений и размерности
  таблицы.
- [ ] Почему на большой выборке очень малый сдвиг может дать маленький p-value.
- [ ] Как объединение редких категорий меняет чувствительность и интерпретацию
  результата.

**Готово, если:** UI/CLI могут показать одновременно χ², исходный и BH-p-value,
Cramér's V и сведения о редких категориях; все крайние случаи JSON-safe.

**Зависимость / передача:** сообщить Павлу точные ключи диагностик для таблицы
метрик и HTML-отчёта.

**Итог дня:** выполнено — …; commit/PR — …; проверено — …; блокеры — …

## 18.09, пятница — защита Adversarial Validation от групповой утечки

**Сделать**

- [ ] Добавить опциональный `adversarial.group_column`. При `None` сохранить
  текущий `StratifiedKFold`; при заданной колонке использовать групповой
  стратифицированный split. Колонка должна существовать в обоих батчах и не
  содержать пропущенных идентификаторов.
- [ ] Получать группы из исходных DataFrame до отбора model features. Никогда не
  подавать group-колонку модели как признак и автоматически исключать её вместе
  с ID/target.
- [ ] Гарантировать, что одна группа не встречается одновременно в train и
  validation; сохранить fixed seed и fold-local preprocessing.
- [ ] Возвращать стратегию split, число пригодных групп и причину `skipped`,
  если групп или классов недостаточно для заданного числа фолдов.
- [ ] Добавить тест с повторяющимися entity ID, проверку отсутствия пересечения
  групп, воспроизводимость и обратную совместимость режима без групп.
- [ ] Сравнить обычный и групповой OOF AUC на маленьком синтетическом примере и
  записать наблюдение без причинной интерпретации.

**Разобраться**

- [ ] Как повтор одной сущности в разных фолдах завышает качество
  классификатора источника.
- [ ] Чем групповой split отличается от исключения ID из признаков.
- [ ] Почему высокий OOF AUC подтверждает различимость батчей, но не падение
  качества основной модели.

**Готово, если:** тест доказывает непересечение групп, обычный сценарий первой
недели не меняется, а при невозможном split модуль объяснимо пропускается.

**Зависимость / передача:** Павлу передать два результата по контракту — для
обычного и группового split — чтобы он добавил стратегию в UI/HTML.

**Итог дня:** выполнено — …; commit/PR — …; проверено — …; блокеры — …

## 19.09, суббота — калибровочные эксперименты

**Сделать**

- [ ] Реализовать воспроизводимый `scripts/run_calibration.py`, который
  перебирает фиксированный список seed, размеры Current и величины числового и
  missingness-сдвига, вызывая публичный `analyze`.
- [ ] Выполнить не меньше 20 контрольных прогонов без внесённого дрейфа и
  несколько уровней сдвига при размерах Current 500, 1200 и 3000 строк.
- [ ] Сохранить компактные машинно-читаемые результаты в `report/experiments/`:
  CSV с метриками и JSON с seed, параметрами, версиями, временем и commit SHA.
- [ ] Рассчитать наблюдаемую долю ложных тревог и долю обнаружений для каждого
  метода. Не называть эти оценки точной гарантией на будущих данных.
- [ ] На основании экспериментов подготовить `configs/demo_calibrated.yaml` с
  персональными порогами для демонстрации и комментариями об их области
  применимости.
- [ ] Обновить notebook: сравнить контроль, слабый и сильный сдвиг; показать
  Cramér's V, источник порога и стратегию ML-split.
- [ ] Передать Павлу зафиксированный сценарий, по которому JSON, HTML и UI
  должны дать одинаковые значения.

**Разобраться**

- [ ] Как размер батча и величина сдвига влияют на мощность теста.
- [ ] Почему 20 seed дают первичную инженерную оценку, а не точный доверительный
  вывод о false-positive rate.
- [ ] Чем подбор демонстрационного порога отличается от production-калибровки
  на исторических данных бизнеса.

**Готово, если:** один скрипт повторяет таблицу результатов, каждое число имеет
seed/commit/config, а выбранные пороги явно помечены как demo-specific.

**Зависимость / передача:** стабильный контрольный сценарий становится общей
приёмочной проверкой Павла для CLI, HTML и Streamlit.

**Итог дня:** выполнено — …; commit/PR — …; проверено — …; блокеры — …

## 20.09, воскресенье — статистический аудит release candidate

**Сделать**

- [ ] Обновиться от `main` после PR Павла и запустить полный набор unit,
  integration и notebook-тестов в чистом Python 3.13 окружении.
- [ ] Выполнить один и тот же calibrated-сценарий через notebook, CLI,
  Streamlit и HTML; сверить метрики, пороги, статусы и число алертов.
- [ ] Проверить, что Cramér's V не выдан за p-value, Feature Importance — за
  причинный эффект, высокий AUC — за доказанное падение основной модели.
- [ ] Объяснить Павлу пороги, BH, Cramér's V, мощность и групповой split; затем
  попросить его воспроизвести инженерный путь от файла до HTML/графика.
- [ ] Провести review инженерного PR Павла и исправить только блокирующие или
  искажающие смысл проблемы.
- [ ] Обновить `docs/plans/mikhail.md`, `docs/experiments.md`,
  `docs/week1_handoff.md` либо новый handoff и `docs/deliverables.md` фактическими
  SHA, результатами и ограничениями.
- [ ] После зелёного CI объединить изменения в `main` и зафиксировать SHA
  кандидата на релиз.

**Разобраться**

- [ ] Какие пороги уже поддерживаются данными эксперимента, а какие остаются
  исключительно иллюстративными.
- [ ] Какие ошибки способны изменить статистический вывод, а какие относятся
  только к представлению результата.
- [ ] Что ещё обязательно для сдачи 27.09: итоговый текст отчёта, скринкаст и
  проверка открытых ссылок.

**Готово, если:** в `main` находится воспроизводимый кандидат на релиз, все
четыре интерфейса согласованы, Павел воспроизводит статистическую часть, а
остаток до 27.09 честно перечислен без скрытых заглушек.

**Итог дня:** выполнено — …; commit/PR — …; проверено — …; блокеры — …
