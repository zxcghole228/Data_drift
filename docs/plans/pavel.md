# Павел — план на 07.09–13.09.2026

Рабочая ветка: `pavel`. Напарник: Михаил, ветка `mikhail`.

**Дедлайн всего проекта: 27.09.2026. Этот файл планирует только первую неделю.** Цель недели — загрузка и проверка данных, первый общий pipeline и базовый дашборд, показывающий реально вычисленные результаты. Полная готовность проекта к 13.09 не требуется.

## Ответственность и договорённости

Твои модули: `ingestion.py`, `schema.py`, `quality.py`, `pipeline.py`, `reporting.py`, `app/`, инструкции установки и будущая проверка Docker. Тесты своего кода пишешь вместе с реализацией. `contracts.py` и схему конфигурации согласовываете вдвоём.

Каждый день заканчивается короткой отметкой: выполненное, commit/PR, проверка и блокер. Если задача не помещается в день, фиксируй реальное состояние и обсуждай перенос внутри текущей недели. Объём за её пределами пока не планируем.

## 07.09, понедельник — окружение, схема и загрузка

**Сделать**

- [x] Вместе с Михаилом разобрать `docs/contracts.md`: согласовать ключи результата, типы, `None`, `skipped` и правила ошибок. Уточнить, какие поля нужны будущему дашборду.
- [x] Поднять Python-окружение, проверить pandas/PyArrow/Streamlit и вместе зафиксировать проверенные версии зависимостей.
- [x] Реализовать `load_table` в `ingestion.py` для локальных CSV/Parquet. Для неподдерживаемого формата и отсутствующего файла дать понятную ошибку.
- [x] Согласовать с Михаилом ожидаемую схему синтетических данных. Загрузить таблицы, которые создаёт его генератор.
- [x] Добавить первые тесты чтения обоих форматов через временные файлы; не класть датасеты в репозиторий.

**Разобраться**

- [x] Различия CSV и Parquet: сохранение типов, пропуски, разделители, кодировки.
- [x] Что такое schema validation, почему тип столбца важен для выбора проверки.
- [x] Разница между библиотечным API, файловым загрузчиком и пользовательским интерфейсом.

**Готово, если:** оба формата читаются, ошибки загрузки объяснимы, ты можешь получить DataFrame по командам генерации Михаила.

**Зависимость / передача:** пока генератор не готов, допустим маленький DataFrame, созданный внутри теста. Схема и контракт согласуются в этот день.

**Итог дня:** выполнено — окружение, схема demo-данных и загрузчик CSV/Parquet; commit/PR — `9e0f2ca`, merge `1f1820b`; проверено — временные файлы обоих форматов; блокеры — нет.

## 08.09, вторник — проверка схемы

**Сделать**

- [x] Реализовать `validate_schema` в `schema.py`: отсутствующие/лишние колонки, несовместимые типы и дублирующиеся имена колонок.
- [x] Реализовать чтение и валидацию согласованной конфигурации, в том числе понятные ошибки неизвестных или некорректных параметров.
- [x] Зафиксировать правила для float/int, pandas nullable-типов и категориальных значений. Ошибочные строки в числовом столбце не исправлять молча.
- [x] Написать тесты совпадающей схемы, отсутствующей колонки, лишней колонки, неверного типа, пустого DataFrame и повторяющихся названий.
- [x] Передать Михаилу формат ошибок и список признаков, которые можно безопасно анализировать.

**Разобраться**

- [x] `int64`, `float64`, nullable `Int64`, `object`, `string`, `category`: чем физический dtype отличается от смысла признака.
- [x] Что проверять до расчёта статистики и почему валидация не должна незаметно изменять данные.
- [x] Как отделять неверную конфигурацию от проблемы в загруженном датасете.

**Готово, если:** нарушения схемы возвращаются в согласованном формате, а корректные колонки остаются доступными для последующего анализа.

**Итог дня:** выполнено — runtime config и Schema Validation; commit/PR — `4ac5507`, merge `fedf689`; проверено — корректные и ошибочные схемы; блокеры — нет.

## 09.09, среда — Data Quality

**Сделать**

- [x] Реализовать пропуски по каждой колонке: количество, долю для Reference/Current и изменение в процентных пунктах.
- [x] Реализовать число и долю дубликатов строк; описать знаменатель и принятое определение дубликата.
- [x] Добавить нарушения заданных min/max и отдельную обработку бесконечностей в числовых признаках.
- [x] Формировать результаты и quality-алерты по контракту; не удалять проблемные строки из входного DataFrame.
- [x] Написать тесты на рост пропусков с 2% до 7%, полностью пропущенную колонку, дубликаты, значения на границе и вне диапазона, пустую таблицу.

**Разобраться**

- [x] Разница процента и процентного пункта; как считать абсолютные и относительные величины.
- [x] Разница дубликатов целых строк и повторения отдельного ID. Какие именно дубликаты считаются в текущем прототипе.
- [x] Почему допустимые min/max задаются предметными правилами, а не автоматически равны минимуму и максимуму Reference.

**Готово, если:** искусственные нарушения обнаруживаются с понятными числами и правилами, исходные таблицы после вызова не изменились.

**Зависимость / передача:** передать Михаилу результаты на его сценарии увеличенных пропусков для проверки интерпретации.

**Итог дня:** выполнено — Data Quality и quality-алерты; commit/PR — `7bec7d6`, merge `9c7d836`; проверено — 95 тестов на момент PR; блокеры — нет.

## 10.09, четверг — первый общий pipeline

**Сделать**

- [x] Реализовать `analyze` в `pipeline.py`: конфигурация → схема → качество → доступные статистические методы → единый результат.
- [x] Подключить уже реализованные функции Михаила. Для ещё недоступных модулей возвращать явный статус и причину, не подставлять выдуманные метрики.
- [x] Обеспечить сериализацию результата в JSON без NaN/Infinity и неизменность входных таблиц.
- [x] Добавить интеграционные тесты на нормальный проход и несовместимую схему. В тестах изоляции разрешены явно обозначенные заглушки; демонстрация использует реальные расчёты.
- [x] Вместе с Михаилом запустить первый проход на данных генератора и согласовать устранение обнаруженных несовместимостей.

**Разобраться**

- [x] Роль общего контракта: почему UI должен читать один результат, а не вызывать тесты самостоятельно.
- [x] Ошибка вычисления, пропущенная проверка и отсутствие алерта — разные состояния.
- [x] Ограничения JSON, сериализация типов numpy и политика частичного результата.

**Готово, если:** один вызов API возвращает реальные результаты схемы, качества и доступных методов; пропуски анализа видны и объяснены.

**Итог дня:** выполнено — первый общий pipeline; commit/PR — `ef445d2`, merge `042fd86`; проверено — нормальный/частичный проход, JSON и неизменность входов; блокеры — на момент PR χ²/BH/ML были честно отмечены как недоступные, затем подключены общим change set.

## 11.09, пятница — базовый дашборд

**Сделать**

- [x] Добавить в `app/streamlit_app.py` загрузку двух файлов, выбор/загрузку схемы и явный запуск анализа.
- [x] Показывать число строк, итоговый статус, таблицу признаков, результаты качества и список алертов из общего результата.
- [x] Реализовать в `reporting.py` график числового распределения и сравнение долей категорий. Использовать сопоставимые оси/бины.
- [x] Различать цветом и текстом предупреждение, критическую проблему, ошибку и пропущенную проверку. На графиках указывать, как обработаны пропуски.
- [x] Проверить интерфейс на нормальном сценарии и на ошибочном файле. Сохранить краткие инструкции запуска.

**Разобраться**

- [x] Как Streamlit повторно выполняет сценарий и почему тяжёлый анализ не должен запускаться при каждом изменении элемента интерфейса.
- [x] Плотность, число наблюдений и доля: какую величину показывает график и можно ли сравнивать батчи разного размера.
- [x] Какие сведения нужны пользователю для понимания алерта: признак, метрика, порог, размер выборки, объяснение.

Материал: [Streamlit: установка и запуск](https://docs.streamlit.io/get-started/installation/command-line).

**Готово, если:** пользователь загружает два файла, запускает анализ и видит реальные результаты и минимум два вида графиков; ошибки не скрыты зелёным статусом.

**Итог дня:** выполнено — Streamlit и два типа сопоставимых графиков; commit/PR — `ac90e4d`, merge `883ac69`; проверено — явный запуск, rerun-state, ошибочный файл и YAML; блокеры — ML-представление было добавлено после готовности модуля.

## 12.09, суббота — интеграция ML-модуля и проверка сценариев

**Сделать**

- [x] Подключить реализованный Михаилом Adversarial Validation к pipeline; сделать его запуск управляемым конфигурацией.
- [x] Показать отложенный ROC-AUC, значения по фолдам и важности признаков. Для выключенного/неприменимого модуля показывать причину отсутствия результата.
- [x] Пройти вместе базовые сценарии из `docs/experiments.md`; сверить UI, notebook и словарь результата.
- [x] Проверить загрузку CSV/Parquet, ошибки схемы, пустые батчи, константную колонку и полностью пропущенный признак в общем пути.
- [x] Уточнить README по фактическому запуску и составить перечень оставшихся инженерных вопросов без дат следующих недель.

**Разобраться**

- [x] Что измеряет классификатор источника данных; почему высокий AUC не равен доказанному падению качества основной модели.
- [x] Как различать медленное вычисление, ошибку модуля и отсутствие подходящих данных в интерфейсе.
- [x] Какие настройки окружения мешают напарнику повторить результат.

**Готово, если:** доступный ML-результат корректно включён в общий отчёт, а ещё не реализованная возможность обозначена явно; найденные расхождения заведены и блокирующие исправлены.

**Зависимость / передача:** если ML-модуль ещё не готов, сначала закончить качественную интеграцию статистики и явно отметить этот блокер. Не заменять AUC демонстрационным числом.

**Итог дня:** выполнено — интеграция ML, UI-таблицы, end-to-end edge cases, README и журнал; commit/PR — входит в общий change set 10–13.09; проверено — CSV/Parquet, schema error, empty/constant/all-missing; блокеры — групповой split остаётся ограничением.

## 13.09, воскресенье — воспроизводимость результата недели

**Сделать**

- [x] Запустить текущий проект из чистого виртуального окружения по README; исправить реальные ошибки установки и путей.
- [x] Выполнить содержательные тесты и общий демонстрационный сценарий. Docker-рецепт проверить при наличии среды Docker; иначе явно оставить его непроверенным.
- [ ] Проверить работу Михаила: выполнить notebook по порядку, попросить объяснить статистические решения и сопоставить их с отчётом.
- [ ] Объяснить Михаилу путь от загруженного файла до графика, проверки схемы и обработку ошибок.
- [ ] Объединить проверенные изменения в `main`, обновить этот план и фактическую готовность в `docs/deliverables.md`.
- [x] Зафиксировать, что ещё нужно для HTML/PDF-отчёта, скринкаста, окончательной сборки и открытой сдачи. Задачи на будущие недели пока не расписывать.

**Разобраться**

- [x] Что делает результат воспроизводимым: версии, seed, доступные данные, команды и сохранённая конфигурация.
- [x] Какие функции уже работают, какие имеют ограничения и какие пока остаются заглушками.

**Готово, если:** напарник может запустить промежуточный результат по README, в `main` лежит проверенная версия недели, оставшиеся требования честно отмечены. Окончательная сдача остаётся на 27.09.

**Итог дня:** выполнено — чистая установка, тесты, demo и фиксация остатка до 27.09; commit/PR — ожидает общий PR; проверено — Python 3.13 и последовательный notebook; блокеры — взаимный устный разбор, merge и Docker-сборка остаются действиями участников.
